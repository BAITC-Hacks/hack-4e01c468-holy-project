You are ML worker Luna xhigh. Read AGENTS.md, brief/board, skills/backend.md, applicable TDD skill. Approved implementation, no new approval. Read plan docs/superpowers/plans/2026-09-23-wind-power-agent.md sections 1–3, T4 and T5. OWN features.py, models.py, evaluation.py under wind_forecast; tests/test_features.py, test_models.py, test_evaluation.py; .hackflow/reports/ml.md. No shared files, commits, subagents. Implement T4 then T5 sequentially, tests and report. .venv has pandas 3/numpy/CatBoost/pytest. Contracts/config from T1 are authoritative. Data/weather workers own upstream.

Freeze exact FeaturePipeline.build, training_rows, FEATURE_COLUMNS, ForecastModel.fit/predict/save/load, BaselineModel.fit/predict, score, rolling_backtest contracts from plan. Important: Predictor metadata timestamp+keys travel through predict results; use Prediction(rows,model_name,diagnostics) dataclass. All training targets complete <=cutoff. Features never read future observations. Demo archived/synthetic weather can train demo models only, competition guard must propagate. Models rows include request metadata; calibration strictly historical. Baseline predicts numeric rows with turbine_id,valid_time,lead_hours,p10,p50,p90. Expose useful calibration/selection helper API if needed and document for integration. Training defaults CPU threads=4. Actual backtest full 30 folds may be expensive; caching and configurable test iterations, no silent leakage shortcut. Report exact APIs/settings and tests; don't spend time installing deps or waiting for upstream, use local fixtures. All implemented code must be usable by next integration worker.
