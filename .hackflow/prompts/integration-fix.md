Luna xhigh integration review-fix worker. Read AGENTS.md, brief/board, backend skill and TDD; original integration worker handed off before your launch. OWN service.py,cli.py, tests/test_cli.py,test_e2e.py,test_acceptance.py, README.md; ALSO app.py and tests/test_app_entry.py for explicit offline web setting, docs/DEMO.md. UI-state worker owns ui/dashboard.py and test_dashboard.py, do not touch them. Agent-integrity worker owns agent/artifacts, coordinate only via report. No commits/subagents. Report integration-fix.md.

Read .hackflow/reports/integration-notes.md and prior integration.md. Resolve all service-owned concrete findings still present, do not redo fixed ones. Mandatory actual staged predictor (prepare/train_or_load/predict) and reuse_identity returning stable config/model identity. Model training occurs after weather gate within trace. Avoid any reuse on changed trained model/config; identity should include active model training fingerprint and dependency/source identity as needed. Baseline selection/calibration only pre-origin held-out labels; absent evidence must remain honest uncalibrated persistence, never future scores. Fix data-quality age, source-aware metrics path and cache-only deterministic action.

Backtest metrics identity: demo synthetic must not be attached to real/competition data or to different CSV content. Persist and validate identity+provenance; metadata must survive ArtifactStore normalization (retained coverage is available). Never label demo metrics competition evidence. Label backtest period separately from forecast-target metrics (February unavailable). Test train/backtest/run small end-to-end path and wrong-source metrics rejection.

Explicit offline dashboard option: app.main should support a documented opt-in environment setting e.g. DEMO_OFFLINE=1 passed to Application(offline=True), only when RUN_MODE=demo; never implicit synthetic fallback. Settings.from_env already loads .env. Add entrypoint test via mocks verifying pass-through and competition refusal, update README/DEMO exact launch command and clarify RUN_MODE=demo alone does NOT select offline source. No dependency changes. Root will run actual browser with this flag and acceptance artifacts.

Use regressions first and focused CLI/E2E/acceptance/app-entry tests. No full heavy Jan backtest (root already running30fold250iters in artifacts/acceptance/models). Do not overwrite root model/runs output. Report any upstream requirement blockers with exact evidence.
