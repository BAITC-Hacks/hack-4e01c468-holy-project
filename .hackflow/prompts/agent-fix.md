Fix T6 review, Luna xhigh, original agent worker finished. Read .hackflow/reports/review-agent.md and skills/backend.md plus TDD. OWN agent.py,llm.py,artifacts.py,tests/test_agent.py,test_llm.py,test_artifacts.py; additionally weather.py ONLY a minimal public read_cached(request)->WeatherSnapshot|None method (weather validation worker finished) if needed; report .hackflow/reports/agent-fix.md. Don't touch service.py/CLI/UI/models. No subagents/commits. Integration worker active using current APIs; maintain compatible ForecastAgent constructor/run and ArtifactStore.

Fix refresh offline fallback: provider HTTP attempts remain <=3 total. NEVER call fetch again after exhausted3 attempts. Add/read cache-only provider method that validates timestamp/provenance and no network; offer use_cached_weather|abort on fetch failure when valid cache exists, record reason. retry_weather is only permitted within provider's existing remaining budget, cannot reset it. Fake provider testing boundary support.

Fix auditable stages: support a staged predictor object exposing prepare(history,snapshot,request)->features, train_or_load(history,features,snapshot,request)->model_bundle, predict(model_bundle,features,request)->Prediction. Existing callable predictors remain compatible but transparently labeled opaque, no fake durations claiming real prep/train. Agent records prepare_features/train_or_load_model/generate_forecast actual calls for staged object. Integration worker will adapt to this exact interface. Record check_for_updates and actual fingerprints/reuse outcome; unchanged request should avoid redundant forecast if safely reusable using history/weather/config/model identity. Cache-only failure and reuse must not bypass gates.

Fix invented LLM metrics: generated report derives numerical statements only from deterministic diagnostics. Keep untrusted LLM reason in clearly labeled trace if needed, report deterministic result reason and validated chosen action. Do not copy fabricated metrics into report. Ensure event text no secrets. Add fabricated MAE/confidence regression.

Provide public API details in report. Existing full six-artifact behavior and two-call bound must stay. Focused tests, no live OpenAI calls. Finish autonomously.

Additional cross-module review: before final handoff read .hackflow/reports/integration-notes.md. Models now report quantile_crossing_correction_count and quantile_clipping_count. _success_status must consider these deterministic model counters as well as quality_gate counters: pre-corrected model output still requires degraded status. Add verified-weather regression with model correction diagnostics and otherwise clean rows.
