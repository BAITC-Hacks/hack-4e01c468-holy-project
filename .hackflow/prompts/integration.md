You are integration worker Luna xhigh. User approved full implementation. Read AGENTS.md, brief/board, skills/backend.md, TDD skill, plan sections1–3/T7/T9. OWN wind_forecast/service.py,cli.py; tests/test_cli.py,test_e2e.py,test_acceptance.py; README.md (preserve hackflow instructions), .hackflow/reports/integration.md. Other modules owned by other workers; do NOT change them, report exact upstream problems. No commits/subagents. Use .venv. Orchestrator already installed deps and requirements.lock.txt.

Implement Application and CLI matching plan, wire ACTUAL DataPipeline/FeaturePipeline/BaselineModel/ForecastModel/ForecastAgent/ArtifactStore APIs from source and reports; don't duplicate ML/agent logic. Read .hackflow/reports/ml.md,agent.md,weather.md,data.md before wiring. If report absent inspect code only signatures. Provide settings consistent config.py. Ensure Application.run honors RunRequest.mode, immutable settings paths, UI calls latest/read_run. read_run dict must include forecast DataFrame,manifest,metrics,events,report. UI worker also depends on these names.

CLI prepare/train/backtest/run/simulate with --mode demo|competition, --offline (explicit synthetic weather for repeatable offline demo), --weather-fixture PATH, --origin/--horizon/--refresh, paths options. Accept options after subcommand for usable commands. Offline flag creates synthetic WeatherProvider wrapper for any origin using weather.make_synthetic_weather_snapshot. NEVER call live OpenAI during tests or offline demo; use deterministic analyzer. In live mode absent API key already falls back. Competition must reject synthetic/unverified. Default mode competition (config), dashboard can choose demo.

Training and backtest must be executable, not placeholder methods: assemble daily snapshots for dates in plan or configurable range; build training_rows with safe cutoff, historical calibration, fit/save; rolling backtest for January labels and metrics. Cache serialized prepared history so reruns fast; invalidate on CSV content hashes. Default run auto train/load with fallback if insufficient weather history. Offline demo may train using explicitly synthetic weather features, marked demo-only; never claim real-weather accuracy. Live demo should fetch actual archive via provider. Weather requests bounded per fetch, no whole enormous silent retry loop. Expose optional origin/date/iterations limits for fast acceptance smoke, but keep documented defaults honest. February simulation 28 origins, all96 keys each, no truth inference; index failures and exit nonzero.

Date-boundary ruling: training targets must END <= first calibration origin (Dec15). Origins through Dec14 yield48h targets spilling into Dec16, so explicitly cutoff training_rows at Dec15, not Dec16. ForecastModel.fit rejects overlapping temporal calibration; honor this. .cache/wind already contains actual live first-Feb1 snapshot. Training/calibration fingerprint must distinguish synthetic/live modes. Agent predictor currently wraps prepare/train/generate in one callable; ensure trace reflects actual stages, coordinate any missing stage with orchestrator in report.

Agent fix worker is ADDING staged predictor support: object.prepare(history,snapshot,request)->features; .train_or_load(history,features,snapshot,request)->model_bundle; .predict(model_bundle,features,request)->Prediction. Use this interface for actual trace stages, existing callables remain supported. Read .hackflow/reports/agent-fix.md when available; no direct agent edits. Primary fallback may remain callable. Model APIs unchanged by ongoing ml-fix.

README exact install using requirements.lock.txt plus -e . --no-deps, all CLI commands, single streamlit command, explicit RUN_MODE=demo usage, offline flags, provenance limitation and no February metrics, bounded LLM/cpu. Read tests from modules before authoring fixtures to meet schema. Tests exercise real application with small fixture source CSV or real prepared source limited and transport synthetic. E2E no OpenAI, forced CatBoost fallback, six schema artifacts. Return concise APIs/tests/failures report. Do not mark implemented without running CLI and tests.

Before final handoff re-read .hackflow/reports/integration-notes.md (root review of initial service draft). In particular staged predictor mandatory: move _ensure_model into train_or_load after weather gate; data quality age must use request.origin and only history available before origin, not model.training_cutoff or future rows. Agent-fix still in progress; preserve interface contract stated above.
