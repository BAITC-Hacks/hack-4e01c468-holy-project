Luna xhigh UI polish/fix. Original UI worker finished. Read skills/frontend.md and .hackflow/reports/ui.md and ui-visual-findings.md. OWN app.py,wind_forecast/ui/,.streamlit/config.toml,tests/test_dashboard.py,docs/DEMO.md,.hackflow/reports/ui-fix.md. No other edits/subagents/commits. Implement all browser findings, inspect provided screenshots using view_image if available. Mobile initial_sidebar_state auto must collapse on390px; semantic h1; map label clipping fix; actual data_quality latest_available_at fallback to compute latest observation age; heading before controls; avoid duplicated status badges. Do NOT hide essential Streamlit sidebar-toggle. Deploy control can be configured away only if simple supported option, not fragile generated selector. Keep polished palette/typography, do not regress current12 UI tests. Agent artifacts actual real preview exists artifacts/runs/run-20260131T190000Z-h48-23c1b86ae840. Use its metadata keys rather than fake data assumptions. Agent code changing separately. UI can read staged events (prepare/train/predict). Ensure no secrets/raw errors in interface. Task is targeted polish, no brand-new redesign. Root runs browser tests after; tests must cover behavior not exact CSS. Report.

Before final report RE-READ ui-visual-findings.md: root added a concrete failed-run vs latest-pointer
state bug. Include regression for newly failed run while latest() intentionally remains previous success.
