Fix T4/T5 review, Luna xhigh. Original ML worker finished. Read .hackflow/reports/review-ml.md, current code, skills/backend.md and testing skill. OWN models.py,evaluation.py, tests/test_models.py,test_evaluation.py, .hackflow/reports/ml-fix.md only. No shared files/subagents/commits. Implement calibration cutoff inference guard; don't drop missing targets before metric scoring so coverage denominator remains requested eligible keys. Keep metric_status unavailable when no observed targets even when rows exist.

Quantile decision: approved spec requires three CatBoost quantile models. Keep all three and USE raw p10/p50/p90 in predictions, with finite checks, clipping and documented crossing correction diagnostics. Residual calibration remains for baselines (and documented optional fallback), not a silent replacement of all CatBoost intervals. No single-model cutline chosen by orchestrator: training performance currently acceptable. Mark uncertainty_method accurately. Avoid changing public constructors/save-prefix API, integration worker using them. Regression tests for all findings and quantile outputs. Run focused suite, report clear evidence. Preserve other workers' code.
