Luna xhigh coding worker. Read AGENTS.md, brief/board, backend skill, TDD. OWN wind_forecast/agent.py,artifacts.py and tests/test_agent.py,test_artifacts.py only; report agent-integrity-fix.md. Previous agent-fix worker has completed before your launch. No commits/subagents, no service/UI/model edits. Read prior agent-fix report and integration-notes.md items1,8.

Fix two concrete reviewed correctness defects with regressions first:
1. Models pre-correct crossing/clipping and report diagnostics quantile_crossing_correction_count / quantile_clipping_count. Agent _success_status only considers quality_gate counters, which are zero after model corrections. These model counters must force degraded on otherwise verified clean output. Test both counters independently.
2. artifacts.persist final-lock _find_matching trusts manifest identity without file integrity verification. Early check_for_updates properly misses a run after forecast.csv removed, but persist same inputs reuses broken directory. Repro via tests _inputs helper: persist, rename forecast.csv to .saved, check_for_updates->None, persist->currently reused True with no forecast. Validate complete required artifacts/hashes/forecast grid in final-lock reuse; corrupt candidate must be skipped and valid unique replacement version published (don't overwrite original). Test missing/corrupt forecast and other required hashed artifact.

Keep existing two-call bound, failed pointer semantics, staged predictor and fingerprint reuse behavior. Run focused agent/artifact tests and report. No live OpenAI/network.
