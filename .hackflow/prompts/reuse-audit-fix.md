Luna xhigh narrow audit fix. Read AGENTS.md, brief/board, skills/backend.md,TDD. OWN only agent.py,artifacts.py,tests/test_agent.py,test_artifacts.py and report reuse-audit-fix.md. Agent-integrity worker completed; root33focusedtests passed and Astra independently accepted integrity/counter fixes. No commits/subagents/other files.

Read review-agent-fix.md residual audit finding. Agent currently early-returns reused result, losing current invocation fetch/validate/cache-fallback/analyzer/check events; store update_checks.jsonl onlyrecords lookup without standardevent fields. Astra agreed fix: preserve original reused run's immutable six artifacts and hashes. Write a separate durable per-invocation trace outside original directory, e.g. root/reuse-events/<uniqueinvocation>.jsonl, with actual current invocation events (sequence,timestamp,duration_ms,state,action,reason,outcome,details), linked reused_run_id and invocation_id. Must include actual refresh failure/cachefallback/analyzer decisions if they happened; do not synthesize durations or rerun forecast. Add ArtifactStore method called by real Agent before early return, maintain compatibility fake stores. Reuse return stays reused=True same run_id. Durable write/secret redaction and safe filename; no arbitrary error text. Existing update_checks log can remain compatibility but full invocation trace must survive.

TDD regression run firstthenunchangedrefresh with staged predictor should reuse noextra modelcalls, original file hashes unchanged, separate trace allactualsteps includescheck hit standardfields and positive/nonnegative realdurations. Also cover refreshfail->validcache->reuse if currentimplementation permits. Targeted tests only; no fullsuite/ML/network. Report APIs/files/checks and finish.
