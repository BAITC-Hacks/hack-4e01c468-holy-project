Luna xhigh scoped code-review fix worker. Read AGENTS/brief/board, skills/backend.md/architecture.md, TDD and verification. Read .hackflow/reports/architecture-batch1.md and review-architecture-batch1.md. Root confirmed Application._predictor_code_identity hashes marshal.dumps(rawcode) including file/line positions, causing incidental cacheinvalidations after extraction. Architecture46659 finished; you now own this fix. API owns presentation and frontend ownsfrontend; don'ttouchthem.

RULING: move existing agent stable code identity algorithm unchanged to a small pure application/code_identity.py public code_fingerprint helper; agent imports/reexports private oldhelper names if used to preserve behavior and tests. service._predictor_code_identity calls same stablehelper instead of rawmarshal codeobjects. No duplicate algorithms. Use same algorithm to preserve existing agent fingerprints. Application service old raw fingerprints intentionally invalidate once; NEVER force reuse results from unknown legacy code. Preserve all existing immutable artifacts/model/weather caches. Documentone-timelegacy invalidation in architecturedesign and report. User notified of one recalculation; globalplan no cross-version oldhash guarantee.

OWN only new application/code_identity.py, service.py hashingfunction/imports, agent.py helperextraction/imports, new tests/test_code_identity.py, docs/architecture/2026-09-23-modular-redesign.md and report architecture-identity-fix.md. No other semantics edits or commits/subagents.

TDD regression FIRST: service identity must be equal when monkeypatching one hashedfunction's __code__.replace(co_firstlineno=...,co_filename=...) without altering semantics, including nested code positions; changing actual body constant or bytecode must differ. Verify helper stable after interpreterwarmup and agent preexistingfingerprints identical algorithm. Add service reuse actualtinyApplication unchangedrefresh coverage, can root /tmp/wind-first-refresh.py. Be careful monkeypatching functions restore refs through pytest monkeypatch, no inplace permanentlymodifying global code. Run tests/test_code_identity.py + test_bootstrap/agent/artifacts/e2e/acceptance, gitdiffcheck. Fullsuite APIunderconstruction mayred outside yourscope; reportexacttests. Finishreportwithchangedfiles/checks/risks.
